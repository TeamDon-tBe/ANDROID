<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DontBe" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/secondary</item>
        <item name="android:windowBackground">@color/gray_1</item>
        <item name="android:textColorHighlight">@color/primary</item>
        <item name="android:textSelectHandleLeft">@drawable/shape_primary_fill_left10_rect</item>
        <item name="android:textSelectHandleRight">@drawable/shape_primary_fill_right10_rect</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DontBe" parent="Base.Theme.DontBe" />
</resources>