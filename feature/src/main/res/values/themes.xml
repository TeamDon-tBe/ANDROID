<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DontBe" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/gray_1</item>
    </style>

    <style name="Theme.DontBe" parent="Base.Theme.DontBe" />

    <style name="Theme.DontBe.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item> <!-- 스플래시 화면 배경 색상 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_symbol_background
        </item> <!-- 스플래시 화면 아이콘 -->
        <item name="postSplashScreenTheme">@style/Base.Theme.DontBe
        </item> <!-- 스플래시 화면 이후 적용할 테마 지정 -->
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerSize50Percent" parent="">
        <item name="cornerSize">50%</item>
    </style>
</resources>
